<main class="create-report-page">
  <h1>Create a Crime Report</h1>

  {{#if error}}
    <p class="error">{{error}}</p>
  {{/if}}

  <form
    action="/reports"
    method="POST"
    enctype="multipart/form-data"
    id="report-form"
  >
    <div>
      <label for="title">Title*</label>
      <input type="text" id="title" name="title" required minlength="5" value="{{prefill.title}}">
    </div>

    <div>
      <label for="desc">Description*</label>
      <textarea id="desc" name="desc" required minlength="15">{{prefill.desc}}</textarea>
    </div>

    <div>
      <label for="crime">Crime Type*</label>
      <input type="text" id="crime" name="crime" required minlength="3" value="{{prefill.crime}}">
    </div>

    <fieldset>
      <legend>Location*</legend>

      <div>
        <label for="state">State</label>
        <input type="text" id="state" name="state" required minlength="2" pattern="[A-Za-z][A-Za-z .'-]*" value="{{prefill.state}}">
      </div>

      <div>
        <label for="city">City</label>
        <input type="text" id="city" name="city" required minlength="2" pattern="[A-Za-z][A-Za-z .'-]*" value="{{prefill.city}}">
      </div>

      <div>
        <label for="area">Neighborhood</label>
        <input type="text" id="area" name="area" required minlength="2" pattern="[A-Za-z][A-Za-z .'-]*" value="{{prefill.area}}">
      </div>

      <div>
        <label for="zipcode">ZIP Code*</label>
        <input
          type="text"
          id="zipcode"
          name="zipcode"
          required
          pattern="[0-9]{5}"
          title="5-digit ZIP code"
          value="{{prefill.zipcode}}"
        >
      </div>
    </fieldset>

    <div>
      <label for="photos">Photos (optional)</label>
      <input type="file" id="photos" name="photos" accept="image/*" multiple>
    </div>

    <div class="checkbox-field">
      <label>
        <input type="checkbox" name="anonymous" {{#if prefill.anonymous}}checked{{/if}}>
        Post as anonymous
      </label>
    </div>

    <button type="submit">Submit Report</button>
  </form>
  <a href="/" class="back-button">← Back to Home</a>
</main>
