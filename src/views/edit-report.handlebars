<main>
  <h1>Edit a Crime Report</h1>

  {{#if error}}
    <p class="error">{{error}}</p>
  {{/if}}

  {{#with report}}
    <form
      action="/reports/{{_id}}"
      method="POST"
      id="report-form"
    >
      <div>
        <label for="title">Title*</label>
        <input type="text" id="title" name="title" value="{{title}}" required minlength="5">
      </div>

      <div>
        <label for="desc">Description*</label>
        <textarea id="desc" name="desc" required minlength="15">{{desc}}</textarea>
      </div>

      <div>
        <label for="crime">Crime Type*</label>
        <input type="text" id="crime" name="crime" value="{{crime}}" required minlength="3">
      </div>

      <fieldset>
        <legend>Location*</legend>

        <div>
          <label for="state">State</label>
          <input type="text" id="state" name="state" value="{{state}}" required minlength="2" pattern="[A-Za-z][A-Za-z .'-]*">
        </div>

        <div>
          <label for="city">City</label>
          <input type="text" id="city" name="city" value="{{city}}" required minlength="2" pattern="[A-Za-z][A-Za-z .'-]*">
        </div>

        <div>
          <label for="area">Neighborhood / Area</label>
          <input type="text" id="area" name="area" value="{{area}}" required minlength="2" pattern="[A-Za-z][A-Za-z .'-]*">
        </div>

        <div>
          <label for="zipcode">ZIP Code</label>
          <input
            type="text"
            id="zipcode"
            name="zipcode"
            required
            pattern="[0-9]{5}"
            title="5-digit ZIP code"
            value="{{zipcode}}"
          >
        </div>
      </fieldset>

      <div>
        <label>
          <input type="checkbox" name="anonymous" {{#if is_anonymous}}checked{{/if}}>
          Post anonymously
        </label>
      </div>

      <button type="submit">Submit Report</button>
    </form>
  {{/with}}
</main>
